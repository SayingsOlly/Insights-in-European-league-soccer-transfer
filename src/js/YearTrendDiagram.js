function YearTrendDiagram() {
    //this.leagues = d3.select('#team-detail-chart-svg').selectAll('div');
    //
    //var svgBounds = svg.node().getBoundingClientRect(),
    //    xAxisWidth = 100,
    //    yAxisHeight = 70,
    //    figureWidth = svgBounds.width - xAxisWidth,
    //    figureHeight = svgBounds.height - yAxisHeight;
    //
    //this.yearValues = years.map(function (d) { return d.split('') })
    //
    //var xScale = d3.scaleLinear()
    //    .domain(allWorldCupData.map(yearSelector))
    //    .range(allWorldCupData.map(function (d, i) {
    //        return figureWidth - i * figureWidth / dataLength - figureWidth / dataLength / 2;
    //    }));
    //
    //var xAxis = d3.axisBottom();
    //xAxis.ticks(years.length)
    //    .scale(xScale);
    //
    //svg.select("#xAxis")
    //    .style("transform", "translate(" + xAxisWidth + "px," + yAxisHeight + "px) scaleY(-1)")
    //    .call(xAxis)
    //    .selectAll('text')
    //    .style("text-anchor", "end")
    //    .style("transform", "translate(-15px, 10px) rotate(-90deg)");
}